cd backend
.\.venv\Scripts\Activate.ps1
python -m PyInstaller --clean --noconfirm reviewpackets.spec
cd ..


Use this on the build machine (not test machine):

cd "C:\Users\Anurag Srivastava\Desktop\Learning\Practice\ReviewPacket\backend"
& "C:\Users\Anurag Srivastava\AppData\Local\Programs\Python\Python311\python.exe" -m venv .venv
Then activate:

.\.venv\Scripts\Activate.ps1
If python not found on a test machine, that is expected and should not be needed.
Test machine should only run the installer EXE, not create .venv.

If your installed app still needs Python on test machine, it means the packaged backend EXE is not being bundled/started correctly, and we should fix packaging/startup rather than installing Python there.




# Find the PID using port 8000
netstat -ano | findstr :8000

# Kill it (replace <PID>)
taskkill /PID <PID> /F
Then rerun:

powershell

python -m uvicorn backend.main:app --host 127.0.0.1 --port 8000

To allow the authorized signed
- Set-ExecutionPolicy RemoteSigned -Scope CurrentUser

Run this once in PowerShell:

Unblock-File .\build-installer.ps1

Then run:

powershell -ExecutionPolicy Bypass -File .\build-installer.ps1



Step 2: Check if port 4200 is open
Open a new PowerShell and run:

netstat -ano | findstr :4200
If it shows a PID, the server is running. If it shows nothing, ng serve isn’t running.

Step 3: Try 127.0.0.1 explicitly

http://127.0.0.1:4200
Step 4: Run ng serve on all interfaces
Sometimes localhost binding fails. Try:

cd frontend
ng serve --host 0.0.0.0 --port 4200



✅ How to build the installer (full bundle)
1. Build backend (bundles Python runtime)
cd backend
.\.venv\Scripts\Activate.ps1
python -m PyInstaller --clean --noconfirm reviewpackets.spec
cd ..

2. Build Angular UI
cd frontend
npm install
npm run build
cd ..

3. Build Electron installer
npm install
npm run dist
✅ Output you give to users
s
After step 3:

dist\ReviewPackets Setup.exe






